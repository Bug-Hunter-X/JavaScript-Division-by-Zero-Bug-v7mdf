function foo(a, b) {    if (b === 0) {        // Handle division by zero.  Throw an error or return NaN or some other appropriate value.        console.error("Division by zero error!");        return NaN; // Or throw new Error("Division by zero");    } else if (a === 0 || b === 0) {        return 0;    }    return a / b;}console.log(foo(1, 0)); //Output:  Division by zero error! NaNconsole.log(foo(0, 1)); //Output: 0console.log(foo(0, 0)); //Output: Division by zero error! NaN